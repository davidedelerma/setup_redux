React - Redux
===============

This is a project to quickly setup react and redux.

First, install the dependencies. 

in server:

```
npm install
nm start
```

in client:

```
npm install
npm start
```
